import Title from './Title.js'
export default function User_data(props){
const user_Data = props.uData;
    return(
        <div>
            <title user_Data = {user_Data}/>
        </div>
    );
}